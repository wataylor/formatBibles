# gentlerKJB

Java 8 command-line tool that reads text files, creates a `.bak` backup, and replaces tabs with four spaces.

Build:

```bash
mvn package
```

Run:

```bash
java -jar target/gentlerKJB-0.0.1-SNAPSHOT.jar file1.txt file2.txt
```
